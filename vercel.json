{
  "buildCommand": "cd movie && npm run build",
  "outputDirectory": "movie/dist",
  "installCommand": "cd movie && npm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
